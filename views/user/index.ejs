<h1>Listing users</h1>

<table>
  <tr>
    <th>Nome</th>
    <th>Curso</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
<!--
	<% for(user in model) { %>
	<tr>
	  <td><%= model[user].nome %></td>
	  <td><%= model[user].curso %></td>
    <td><a href="/user/show/<%= model[user].id %>">Show</a></td>
    <td><a href="/user/edit/<%= model[user].id %>">Edit</a></td>
	<% } %>
	</tr>
-->

  <% _.each(model, function (user) { %>
  <tr>
    <td><%= user.nome %></td>
    <td><%= user.curso %></td>
    <td><a href="/user/show/<%= user.id %>">Show</a></td>
    <td><a href="/user/edit/<%= user.id %>">Edit</a></td>
     <td><a href="/user/destroy/<%= user.id %>">Delete</a></td>
  <% }) %>
  </tr>
</table>
<br />

<a href="/user/new">New User</a>